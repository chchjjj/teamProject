<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.teamProject.payment.mapper.PaymentMapper">

	<select id="selectCartList" parameterType="hashmap" resultType="com.example.teamProject.payment.model.Payment"> 
		SELECT * 
		FROM CART_TBL C
		INNER JOIN CART_OPTION_TBL CO ON C.CART_ID = CO.CART_ID
		INNER JOIN SELLER_INFO_TBL SI ON C.STORE_ID = SI.STORE_ID
		INNER JOIN PRODUCT_TBL P ON C.PRO_NO = P.PRO_NO
		WHERE C.CART_ID IN
		<foreach collection="list" item="item" index="index" open="(" close=")" separator=",">
			#{item} 
		</foreach>
	</select>
	
	<select id="selectOrderList" parameterType="hashmap" resultType="com.example.teamProject.payment.model.Payment"> 
		SELECT * 
		FROM ORDER_TBL O
        INNER JOIN ORDER_DETAIL_TBL OD ON O.ORDER_ID = OD.ORDER_ID 
        
        <!--옵션 정보까지 불러올려면 아래 줄이 필요함--> 
        INNER JOIN ORDER_OPTION_TBL OO ON OD.ORDER_DETAIL_ID = OO.ORDER_DETAIL_ID
		
		WHERE O.ORDER_ID IN
		<foreach collection="list" item="item" index="index" open="(" close=")" separator=",">
			#{item} 
		</foreach>
	</select>
	
	<delete id="deleteCartList" parameterType="hashmap">
		DELETE FROM CART_TBL
		WHERE CART_NO IN
		<foreach collection="list" item="item" index="index" open="(" close=")" separator=",">
			#{item}
		</foreach>
		
	</delete>
	
	
	<update id="updateDelivery" parameterType="hashmap">
		UPDATE ORDER_TBL
		SET DELIVERY_TYPE = #{deliveryType}
		WHERE ORDER_ID = #{orderId}
	</update>
	
	<select id="selectOrderAddress" parameterType="hashmap" resultType="com.example.teamProject.payment.model.Payment"> 
		SELECT * 
		FROM ORDER_TBL
		WHERE ORDER_ID = #{orderId}
		
	</select>
	
	<insert id="insertPayment" parameterType="hashmap">
	    INSERT INTO PAYMENT_TBL 
	    VALUES (
			PAYMENT_SEQ.NEXTVAL, 
			#{orderId},
			#{totalPrice},
			NULL, 
	         0, 
	        'N', 
	         SYSDATE,
	         'Y' 
		)
	</insert>
	
	<insert id="insertAddress" parameterType="hashmap">
	    INSERT INTO USER_ADDRESS(ADDRESS_ID, USER_ID, FULL_ADDRESS, PHONE, CDATETIME_AT, UDATETIME_AT)
	    VALUES (
			SEQ_USER_ADDRESS.NEXTVAL, 
			#{userId},
			#{addr},
			#{phone}, 
	         SYSDATE,
	         SYSDATE
	         
		)
	</insert>
	
	<select id="selectUserAddress" parameterType="hashmap" resultType="com.example.teamProject.payment.model.Payment"> 
		SELECT ADDRESS_ID, USER_ID, FULL_ADDRESS, PHONE, CDATETIME_AT, UDATETIME_AT
		FROM USER_ADDRESS
		WHERE USER_ID = #{userId}
	</select>
	
	<delete id="deleteUserAddress" parameterType="hashmap">
		DELETE FROM USER_ADDRESS
		WHERE ADDRESS_ID = #{addressId}
	</delete>
	
	<update id="updateOrderAddress" parameterType="hashmap">
		UPDATE ORDER_TBL
		SET FULL_ADDRESS = #{fullAddress}
		WHERE ORDER_ID = #{orderId}
	</update>
	
	<update id="updateOrderStatus" parameterType="hashmap">
		UPDATE ORDER_TBL
		SET STATUS = 'P'
		WHERE ORDER_ID = #{orderId}
	</update> 
	
	<update id="updateDeliveryWishDeli" parameterType="hashmap">
	    UPDATE DELIVERY_TBL
		SET WISH_DELI = TO_DATE(#{selectedDate}, 'YYYY-MM-DD HH24:MI')
		WHERE ORDER_ID = #{orderId}
	</update>
	
	<update id="updatePickUpDate" parameterType="hashmap">
	    UPDATE PICKUP_TBL
		SET 
			PICKUP_DATE = TO_DATE(#{selectedDate}, 'YYYY-MM-DD HH24:MI'), 
			PICK_TIME = TO_DATE(#{selectedDate}, 'YYYY-MM-DD HH24:MI')
		WHERE ORDER_ID = #{orderId}
	</update>
	
</mapper>